\input{./._relpath-to-latexroot.ltx}
\documentclass[\latexroot/\projectname]{subfiles}
\input{\latexroot/@resources/subfile-setup.ltx}

\begin{document}

% =====================================================================
% Placeholder.  The original HAFiscal template shipped a figure showing
% cumulative multipliers from a HANK-SAM extension.  The de Ferra, Mitman,
% and Romei (2020) paper does NOT contain a multipliers figure, so the
% HAFiscal content has been removed.  The filename is retained so that
% the build scaffolding remains stable while we decide what (if anything)
% the HANK-related subfile of this REMARK should show.
%
% Speculative: one natural repurposing would be to generate a figure
% comparing aggregate consumption IRFs under the four experiments in
% Section 7 of de Ferra et al. (2020) (foreign interest rate, credit
% supply, foreign demand, productivity).  That is not implemented here.
% =====================================================================

\begin{figure}[htb]
  \centering
  \caption{Placeholder---no corresponding figure in \citet{de-Ferra2019-co}%
    \whenintegrated{\label{fig:deFerra2020_placeholder_multipliers}}}
  \fbox{\parbox{.7\textwidth}{%
      \footnotesize\centering
      This subfile is a placeholder.  It previously rendered a HAFiscal
      multipliers figure that has no counterpart in the de Ferra,
      Mitman, and Romei (2020) paper this REMARK reproduces.
  }}
\end{figure}

\smartbib

\end{document}
